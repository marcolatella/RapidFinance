name: RapidFinance

channels:
  - defaults

dependencies:
  - pip:
    - matplotlib
    - setuptools
    - requests
    - pandas
    - PyYAML
    - numpy
    - plotly

prefix: /opt/anaconda3/envs/RapidFinance